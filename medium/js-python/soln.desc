s is the substitute feature in Vim. You can view its details in `:help :s`.

All of these commands are doing simple substitutions to reach from the source to the target format.

The tricky part here may be `\(let\|var\)` which is a regex that matches on either `let` or `var`. You can see more help on that here: [tutorial](https://www.regular-expressions.info/brackets.html)
